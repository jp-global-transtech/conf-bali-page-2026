@extends('layouts.app')

@section('content')
<div x-data="{ currentTab: 'overview' }">
    {{-- Sticky Announcement Bar - Hidden on mobile --}}
    <div class="hidden lg:block sticky top-0 z-[70] bg-gradient-to-r from-emerald-600 via-emerald-700 to-blue-900 shadow-lg py-2 px-4" role="banner" id="announcement-bar" style="height: 48px;">
        <div class="max-w-7xl mx-auto">
            <div class="flex flex-col sm:flex-row items-center justify-between gap-2 text-center sm:text-left">
                <div class="flex items-center gap-2 flex-wrap justify-center sm:justify-start">
                    <span class="inline-flex items-center gap-1.5 px-3 py-1 bg-white/20 backdrop-blur-sm rounded-full text-xs sm:text-sm font-medium text-white">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                        </svg>
                        <span>Early Bird Registration ends June 15, 2026!</span>
                    </span>
                </div>
                <div class="flex items-center gap-3">
                    <a href="{{ route('registrations') }}" class="text-xs sm:text-sm font-semibold text-white hover:text-emerald-200 transition-colors flex items-center gap-1">
                        Register Now
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                        </svg>
                    </a>
                </div>
            </div>
        </div>
    </div>

    {{-- New Navbar Component --}}
    <x-navbar />

    <!-- Hero Section -->
    <section id="overview" class="relative min-h-screen flex items-center justify-center pt-16" style="background-color: #123945;">
        <div class="absolute inset-0 opacity-10">
            <img src="/hero-bg.jpg" alt="" class="w-full h-full object-cover">
        </div>
        <div class="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 text-center">
            <div class="inline-block mb-4">
                <span class="badge badge-outline border-white/30 text-white px-4 py-3 text-sm">{{ $conference['badge'] ?? 'Conference Bali 2026' }}</span>
            </div>
            <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold text-white mb-6">
                {{ $conference['name'] ?? 'Bali 2026 International Conference' }}
            </h1>
            <p class="text-xl sm:text-2xl text-blue-100 mb-8 max-w-3xl mx-auto">
                {{ $conference['theme'] ?? 'Building a Sustainable Future: Innovations in Climate Resilience and Green Technology' }}
            </p>
            <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-8">
                <div class="flex items-center gap-2 text-white">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                    </svg>
                    <span class="text-lg">{{ $conference['event_date'] ?? 'August 24-25, 2026' }}</span>
                </div>
                <div class="flex items-center gap-2 text-white">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                    </svg>
                    <span class="text-lg">{{ $conference['location'] ?? 'Bali, Indonesia' }}</span>
                </div>
            </div>
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a href="{{ route('registrations') }}" class="btn btn-lg bg-white text-blue-900 hover:bg-blue-50 border-0">Register Now</a>
                <a href="#topics" class="btn btn-lg btn-outline border-white text-white hover:bg-white/10">Explore Topics</a>
            </div>
        </div>
        <div class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-white to-transparent"></div>
    </section>

    <!-- About / Overview Section -->
    <section class="py-16 sm:py-20 px-4 sm:px-6 bg-white">
        <div class="max-w-6xl mx-auto">
            <div class="grid lg:grid-cols-2 gap-12 items-center">
                <div>
                    <span class="text-sm uppercase tracking-wider text-blue-900 font-semibold">About the Conference</span>
                    <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mt-2 mb-6">Shaping the Future of Sustainability</h2>
                    <p class="text-gray-600 text-lg leading-relaxed mb-6">
                        The Bali 2026 International Conference on Climate Resilience and Green Technology brings together leading researchers, policymakers, and industry professionals from around the world to share innovative solutions for building a sustainable future.
                    </p>
                    <p class="text-gray-600 text-lg leading-relaxed mb-6">
                        Join us for two days of insightful presentations, interactive workshops, and networking opportunities as we explore the latest advances in climate adaptation, renewable energy, green infrastructure, and environmental policy.
                    </p>
                    <div class="grid grid-cols-3 gap-4">
                        <div class="text-center p-4 bg-blue-50 rounded-xl">
                            <div class="text-3xl font-bold text-blue-900">500+</div>
                            <div class="text-sm text-gray-600">Delegates</div>
                        </div>
                        <div class="text-center p-4 bg-emerald-50 rounded-xl">
                            <div class="text-3xl font-bold text-emerald-700">50+</div>
                            <div class="text-sm text-gray-600">Speakers</div>
                        </div>
                        <div class="text-center p-4 bg-purple-50 rounded-xl">
                            <div class="text-3xl font-bold text-purple-700">30+</div>
                            <div class="text-sm text-gray-600">Countries</div>
                        </div>
                    </div>
                </div>
                <div class="relative">
                    <div class="aspect-video rounded-2xl shadow-2xl overflow-hidden" style="background: linear-gradient(135deg, #0a2a69 0%, #115e59 100%);">
                        <div class="absolute inset-0 flex items-center justify-center">
                            <div class="text-center text-white">
                                <svg class="w-20 h-20 mx-auto mb-4 opacity-50" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z" />
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                                <p class="text-lg">Conference Video Coming Soon</p>
                            </div>
                        </div>
                    </div>
                    <div class="absolute -bottom-6 -right-6 w-32 h-32 bg-gradient-to-br from-emerald-400 to-blue-500 rounded-2xl -z-10"></div>
                    <div class="absolute -top-6 -left-6 w-24 h-24 bg-gradient-to-br from-purple-400 to-pink-500 rounded-full -z-10"></div>
                </div>
            </div>
        </div>
    </section>

    <!-- Topics Section -->
    <section id="topics" class="py-16 sm:py-20 px-4 sm:px-6 bg-gradient-to-b from-gray-50 to-white">
        <div class="max-w-6xl mx-auto">
            <div class="text-center mb-12">
                <span class="text-sm uppercase tracking-wider text-blue-900 font-semibold">Conference Topics</span>
                <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mt-2">Explore Our Focus Areas</h2>
                <p class="text-gray-600 mt-4 max-w-2xl mx-auto">Our conference covers a wide range of topics related to climate resilience and green technology.</p>
            </div>

            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
                @foreach($topics ?? [] as $topic)
                <div class="card bg-white border border-gray-100 shadow-lg hover:shadow-xl transition-shadow duration-300">
                    <div class="card-body p-6">
                        <div class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center mb-4">
                            <svg class="w-6 h-6 text-blue-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                        </div>
                        <h3 class="card-title text-xl font-bold text-gray-900 mb-2">{{ $topic['title'] }}</h3>
                        <p class="text-gray-600">{{ $topic['description'] }}</p>
                    </div>
                </div>
                @endforeach
            </div>
        </div>
    </section>

    <!-- Key Dates Section -->
    <section class="py-16 sm:py-20 px-4 sm:px-6 bg-white">
        <div class="max-w-4xl mx-auto">
            <div class="text-center mb-12">
                <span class="text-sm uppercase tracking-wider text-blue-900 font-semibold">Important Dates</span>
                <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mt-2">Mark Your Calendar</h2>
            </div>

            <div class="space-y-4">
                @foreach($key_dates ?? [] as $date)
                <div class="flex items-center gap-4 p-4 bg-gray-50 rounded-xl hover:bg-blue-50 transition-colors">
                    <div class="flex-shrink-0 w-24 text-center">
                        <span class="text-xs uppercase {{ $date['status'] === 'active' ? 'text-emerald-600 font-semibold' : 'text-gray-500' }}">
                            {{ $date['status'] === 'active' ? 'Open' : 'Upcoming' }}
                        </span>
                    </div>
                    <div class="flex-1">
                        <h3 class="font-semibold text-gray-900">{{ $date['event'] }}</h3>
                    </div>
                    <div class="text-gray-600 font-medium">{{ $date['date'] }}</div>
                </div>
                @endforeach
            </div>
        </div>
    </section>

    <!-- Schedule Preview -->
    <section id="schedule" class="py-16 sm:py-20 px-4 sm:px-6 bg-gradient-to-b from-gray-50 to-white">
        <div class="max-w-5xl mx-auto">
            <div class="text-center mb-12">
                <span class="text-sm uppercase tracking-wider text-blue-900 font-semibold">Program Schedule</span>
                <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mt-2">Conference Agenda</h2>
            </div>

            <!-- Day 1 -->
            <div class="mb-8">
                <h3 class="text-2xl font-bold text-gray-900 mb-4 flex items-center gap-3">
                    <span class="w-10 h-10 bg-blue-900 text-white rounded-lg flex items-center justify-center">1</span>
                    Day 1 - {{ $schedule['day_1']['date'] ?? 'August 24, 2026' }}
                </h3>
                <div class="space-y-3">
                    @foreach($schedule['day_1']['sessions'] ?? [] as $session)
                    <div class="flex items-start gap-4 p-4 bg-white rounded-xl border border-gray-200 @if($session['type'] === 'keynote') border-l-4 border-l-blue-900 @endif">
                        <div class="flex-shrink-0 w-24 text-sm text-gray-500 font-medium">{{ $session['time'] }}</div>
                        <div class="flex-1">
                            <h4 class="font-semibold text-gray-900">{{ $session['title'] }}</h4>
                            <span class="text-xs uppercase text-gray-400">{{ $session['type'] }}</span>
                        </div>
                    </div>
                    @endforeach
                </div>
            </div>

            <!-- Day 2 -->
            <div>
                <h3 class="text-2xl font-bold text-gray-900 mb-4 flex items-center gap-3">
                    <span class="w-10 h-10 bg-emerald-700 text-white rounded-lg flex items-center justify-center">2</span>
                    Day 2 - {{ $schedule['day_2']['date'] ?? 'August 25, 2026' }}
                </h3>
                <div class="space-y-3">
                    @foreach($schedule['day_2']['sessions'] ?? [] as $session)
                    <div class="flex items-start gap-4 p-4 bg-white rounded-xl border border-gray-200 @if($session['type'] === 'keynote') border-l-4 border-l-emerald-700 @endif">
                        <div class="flex-shrink-0 w-24 text-sm text-gray-500 font-medium">{{ $session['time'] }}</div>
                        <div class="flex-1">
                            <h4 class="font-semibold text-gray-900">{{ $session['title'] }}</h4>
                            <span class="text-xs uppercase text-gray-400">{{ $session['type'] }}</span>
                        </div>
                    </div>
                    @endforeach
                </div>
            </div>

            <div class="text-center mt-8">
                <a href="{{ route('conference.schedule') }}" class="btn btn-outline border-blue-900 text-blue-900 hover:bg-blue-50">Download Full Schedule</a>
            </div>
        </div>
    </section>

    <!-- Venue Section -->
    <section id="venue" class="py-16 sm:py-20 px-4 sm:px-6 bg-white">
        <div class="max-w-6xl mx-auto">
            <div class="grid lg:grid-cols-2 gap-12">
                <div>
                    <span class="text-sm uppercase tracking-wider text-blue-900 font-semibold">Venue & Location</span>
                    <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mt-2 mb-6">{{ $venue['name'] ?? 'Grand Hyatt Bali' }}</h2>
                    <p class="text-gray-600 text-lg mb-6">{{ $venue['description'] ?? '' }}</p>

                    <div class="space-y-4 mb-8">
                        <div class="flex items-start gap-4">
                            <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center flex-shrink-0">
                                <svg class="w-5 h-5 text-blue-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                                </svg>
                            </div>
                            <div>
                                <h4 class="font-semibold text-gray-900">Address</h4>
                                <p class="text-gray-600">{{ $venue['address'] ?? 'Kawasan Nusa Dua, BTDC Lot 5, Benoa, Bali 80363, Indonesia' }}</p>
                            </div>
                        </div>
                        <div class="flex items-start gap-4">
                            <div class="w-10 h-10 bg-emerald-100 rounded-lg flex items-center justify-center flex-shrink-0">
                                <svg class="w-5 h-5 text-emerald-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                            </div>
                            <div>
                                <h4 class="font-semibold text-gray-900">Getting There</h4>
                                <p class="text-gray-600">{{ $venue['getting_there'][0] ?? '20 minutes from Ngurah Rai International Airport (DPS)' }}</p>
                            </div>
                        </div>
                    </div>

                    <a href="{{ route('conference.venue') }}" class="btn btn-primary !bg-conference-dark hover:!bg-conference-dark/80 !text-white border-0">View Venue Details</a>
                </div>

                <div class="bg-gray-100 rounded-2xl aspect-square flex items-center justify-center">
                    <div class="text-center text-gray-400">
                        <svg class="w-20 h-20 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2V9z" />
                        </svg>
                        <p>Interactive Map Coming Soon</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Speakers Section -->
    <section id="speakers" class="py-16 sm:py-20 px-4 sm:px-6 bg-gradient-to-b from-gray-50 to-white">
        <div class="max-w-6xl mx-auto">
            <div class="text-center mb-12">
                <span class="text-sm uppercase tracking-wider text-blue-900 font-semibold">Keynote & Invited Speakers</span>
                <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mt-2">Meet Our Speakers</h2>
            </div>

            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
                @foreach($speakers ?? [] as $speaker)
                <div class="card bg-white border border-gray-100 shadow-lg hover:shadow-xl transition-shadow">
                    <div class="card-body p-6 text-center">
                        <div class="w-24 h-24 bg-gradient-to-br from-blue-100 to-emerald-100 rounded-full mx-auto mb-4 flex items-center justify-center">
                            <svg class="w-12 h-12 text-blue-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                            </svg>
                        </div>
                        <h3 class="text-lg font-bold text-gray-900">{{ $speaker['name'] }}</h3>
                        <p class="text-blue-900 text-sm font-medium mb-2">{{ $speaker['title'] }}</p>
                        <p class="text-gray-500 text-sm mb-3">{{ $speaker['affiliation'] }}</p>
                        <div class="flex flex-wrap gap-1 justify-center">
                            @foreach($speaker['topics'] ?? [] as $topic)
                            <span class="badge badge-sm bg-blue-50 text-blue-900 border-0">{{ $topic }}</span>
                            @endforeach
                        </div>
                    </div>
                </div>
                @endforeach
            </div>

            <div class="text-center mt-8">
                <a href="{{ route('conference.speakers') }}" class="btn btn-outline border-blue-900 text-blue-900 hover:bg-blue-50">View All Speakers</a>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="py-16 sm:py-20 px-4 sm:px-6" style="background: linear-gradient(135deg, #0a2a69 0%, #115e59 100%);">
        <div class="max-w-4xl mx-auto text-center">
            <h2 class="text-3xl sm:text-4xl font-bold text-white mb-6">Ready to Join Us?</h2>
            <p class="text-blue-100 text-lg mb-8">Secure your spot at the premier international conference on climate resilience and green technology.</p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a href="{{ route('registrations') }}" class="btn btn-lg bg-white text-blue-900 hover:bg-blue-50 border-0">Register Now</a>
                <a href="{{ route('contact') }}" class="btn btn-lg btn-outline border-white text-white hover:bg-white/10">Contact Us</a>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="px-4 py-8 sm:px-6 sm:py-10 text-white" style="background: linear-gradient(135deg, #0a2a69 0%, #115e59 100%);">
        <div class="mx-auto flex max-w-5xl flex-col items-center gap-6 text-center">
            <nav class="flex flex-col items-center gap-3 sm:flex-row sm:flex-wrap sm:justify-center sm:gap-x-6 sm:gap-y-3">
                <a href="{{ route('home') }}" class="link link-hover text-sm text-blue-50 hover:text-white">Home</a>
                <a href="#overview" class="link link-hover text-sm text-blue-50 hover:text-white">Overview</a>
                <a href="#topics" class="link link-hover text-sm text-blue-50 hover:text-white">Topics</a>
                <a href="#schedule" class="link link-hover text-sm text-blue-50 hover:text-white">Schedule</a>
                <a href="#venue" class="link link-hover text-sm text-blue-50 hover:text-white">Venue</a>
                <a href="#speakers" class="link link-hover text-sm text-blue-50 hover:text-white">Speakers</a>
                <a href="{{ route('registrations') }}" class="link link-hover text-sm text-blue-50 hover:text-white">Register</a>
            </nav>
            <aside>
                <p class="max-w-md text-sm leading-relaxed text-blue-100">&copy; {{ date('Y') }} JP Global Transtech. All rights reserved.</p>
            </aside>
        </div>
    </footer>
</div>
@endsection
